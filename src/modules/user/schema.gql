input userData {
  email: String!
  username: String!
  password: String!
  role: String
}

# type authResponse {
#   token: String
# }

extend type Mutation {
  signup(input: userData!): Boolean
  login(input: userData!): Boolean
}
input UserData {
  email: String!
  username: String!
  password: String!
  role: String
}

type User {
  email: String!
  username: String!
  role: String
  id: ID!
}

type AuthResponse {
  token: String
}

extend type Query {
  me: User
  users: [User]
  user(id: ID!): User
}

extend type Mutation {
  signup(input: UserData!): AuthResponse
  login(input: UserData!): AuthResponse
}
